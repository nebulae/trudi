# secrets
.env

# python
__pycache__/
*.pyc
*.pyo
*.pyd
.pytest_cache/
.coverage
htmlcov/
dist/
build/
*.egg-info/

# case outputs — never commit investigation artifacts (root-level only)
/analysis/
/exports/
/reports/
*.log
*.csv
# exclude generated/trace JSON but keep config JSON in the repo
*_trace.json
mft*.json

# OS
.DS_Store

# Keep the committed demo-run bundle for judges (overrides *_trace.json / *.log / *.csv above)
!docs/demo/**
