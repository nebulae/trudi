-r requirements.txt
pytest>=8.0.0
pytest-cov>=5.0.0
pytest-xdist>=3.5.0   # `pytest -n auto`: the suite is I/O-bound (trace flushes); 32 cores → ~30 s instead of ~2 min serial
pytest-asyncio>=0.23.0   # async middleware tests (@pytest.mark.asyncio) need this
